[project]
name = "resumer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "markdown>=3.10.2",
    "python-frontmatter>=1.1.0",
    "playwright>=1.50.0",
]
